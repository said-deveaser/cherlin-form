import {CreateFormReturnType} from './types/CreateForm'

export const createForm = <FormValues extends Record<string, any>>(): CreateFormReturnType => {
  const _formValues = {}

  return {}
}

// createForm()
